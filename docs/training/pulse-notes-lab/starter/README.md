# PulseNotes Starter

空绿场练习项目。不要在这里手写完整实现——用 Skills 流程生成 PRD 与任务。

## 建议

1. 阅读 lab 的 `scenario.md`
2. 触发 `prd-author` → `prd-grill` → `prd-build-loop`
3. 测试命令：`npm test`（Node 内置 test runner）

实现时可以把代码放在 `src/`，测试放在 `test/`。
