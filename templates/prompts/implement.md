## Context
Study: specs/README.md, CONTEXT.md

## Task
1. Read `specs/implementation-plans/active-plan.md`
2. Find FIRST unchecked `- [ ]` task
3. Implement ONLY that task using `tdd`

## On Success
Mark `[x]`, log to `.ralph-logs/session.log`, commit, STOP

## On Failure
Max 3 retries, then leave `[ ]` and STOP
